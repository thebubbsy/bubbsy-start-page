services:
  - type: web
    name: bubbsy-start-page
    runtime: python
    buildCommand: ""
    startCommand: python server.py
    envVars:
      # Optional: enables "Sign in with Google" (see README → Accounts configuration).
      - key: GOOGLE_CLIENT_ID
        sync: false
